apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qdrant-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "standard"  # Adjust based on your cluster's storage classes
  resources:
    requests:
      storage: 20Gi