apiVersion: v1
kind: Service
metadata:
  name: upload-service
  namespace: default
  labels:
    app: upload-service
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: upload-service